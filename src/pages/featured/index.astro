---
import InstallBox from "~/components/InstallBox.astro";
import ResourceLinks from "~/components/ResourceLinks.astro";
import SectionHeading from "~/components/SectionHeading.astro";
import SeoMeta from "~/components/SeoMeta.astro";
import StyleGate from "~/components/StyleGate.astro";
import SiteFooter from "~/components/SiteFooter.astro";
import SiteHeader from "~/components/SiteHeader.astro";
import {
  airflow,
  airflowInstallCmd,
  clickhouse,
  clickhouseInstallCmd,
  html,
  installCmd,
  k8s,
  k8sInstallCmd,
  k3s,
  k3sInstallCmd,
  once,
  rama,
  ramaInstallCmd,
  vaultwarden,
  vaultwardenInstallCmd,
  walter,
  walterInstallCmd,
} from "~/data/landing";

const title = "Featured — Colors";
const description = "A curated showcase of production infrastructure and platform automation built with the Colors SDK.";
const featuredPackageSkills = [
  { label: "K8s", href: "#k8s" },
  { label: "K3s", href: "#k3s" },
  { label: "ClickHouse", href: "#clickhouse" },
  { label: "Airflow", href: "#airflow" },
  { label: "Rama", href: "#rama" },
  { label: "Once", href: "#once" },
  { label: "Vaultwarden", href: "#vaultwarden" },
  { label: "Walter", href: "#walter" },
];
const step = (i: number) => `Step ${String(i + 1).padStart(2, "0")}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <SeoMeta title={title} description={description} path="/featured" image="/og-featured-blue-v1.png" imageAlt="Featured Package Skills built with Colors" />
    <StyleGate />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <script
      is:inline
      async
      src="https://www.googletagmanager.com/gtag/js?id=G-4VKP1WY4QJ"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());
      gtag("config", "G-4VKP1WY4QJ");
    </script>
    <style is:global>
      /* cyrillic-ext */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-400-cyrillic-ext.woff2") format('woff2');
        unicode-range: U+0460-052F, U+1C80-1C8A, U+20B4, U+2DE0-2DFF, U+A640-A69F, U+FE2E-FE2F;
      }
      /* cyrillic */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-400-cyrillic.woff2") format('woff2');
        unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
      }
      /* vietnamese */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-400-vietnamese.woff2") format('woff2');
        unicode-range: U+0102-0103, U+0110-0111, U+0128-0129, U+0168-0169, U+01A0-01A1, U+01AF-01B0, U+0300-0301, U+0303-0304, U+0308-0309, U+0323, U+0329, U+1EA0-1EF9, U+20AB;
      }
      /* latin-ext */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-400-latin-ext.woff2") format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+0304, U+0308, U+0329, U+1D00-1DBF, U+1E00-1E9F, U+1EF2-1EFF, U+2020, U+20A0-20AB, U+20AD-20C0, U+2113, U+2C60-2C7F, U+A720-A7FF;
      }
      /* latin */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-400-latin.woff2") format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      /* cyrillic-ext */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 500;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-500-cyrillic-ext.woff2") format('woff2');
        unicode-range: U+0460-052F, U+1C80-1C8A, U+20B4, U+2DE0-2DFF, U+A640-A69F, U+FE2E-FE2F;
      }
      /* cyrillic */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 500;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-500-cyrillic.woff2") format('woff2');
        unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
      }
      /* vietnamese */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 500;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-500-vietnamese.woff2") format('woff2');
        unicode-range: U+0102-0103, U+0110-0111, U+0128-0129, U+0168-0169, U+01A0-01A1, U+01AF-01B0, U+0300-0301, U+0303-0304, U+0308-0309, U+0323, U+0329, U+1EA0-1EF9, U+20AB;
      }
      /* latin-ext */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 500;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-500-latin-ext.woff2") format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+0304, U+0308, U+0329, U+1D00-1DBF, U+1E00-1E9F, U+1EF2-1EFF, U+2020, U+20A0-20AB, U+20AD-20C0, U+2113, U+2C60-2C7F, U+A720-A7FF;
      }
      /* latin */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 500;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-500-latin.woff2") format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      /* cyrillic-ext */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-600-cyrillic-ext.woff2") format('woff2');
        unicode-range: U+0460-052F, U+1C80-1C8A, U+20B4, U+2DE0-2DFF, U+A640-A69F, U+FE2E-FE2F;
      }
      /* cyrillic */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-600-cyrillic.woff2") format('woff2');
        unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
      }
      /* vietnamese */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-600-vietnamese.woff2") format('woff2');
        unicode-range: U+0102-0103, U+0110-0111, U+0128-0129, U+0168-0169, U+01A0-01A1, U+01AF-01B0, U+0300-0301, U+0303-0304, U+0308-0309, U+0323, U+0329, U+1EA0-1EF9, U+20AB;
      }
      /* latin-ext */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-600-latin-ext.woff2") format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+0304, U+0308, U+0329, U+1D00-1DBF, U+1E00-1E9F, U+1EF2-1EFF, U+2020, U+20A0-20AB, U+20AD-20C0, U+2113, U+2C60-2C7F, U+A720-A7FF;
      }
      /* latin */
      @font-face {
        font-family: 'IBM Plex Mono';
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: url("/fonts/ibm-plex-mono-600-latin.woff2") format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      /* cyrillic-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 400;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic-ext.woff2") format('woff2');
        unicode-range: U+0460-052F, U+1C80-1C8A, U+20B4, U+2DE0-2DFF, U+A640-A69F, U+FE2E-FE2F;
      }
      /* cyrillic */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 400;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic.woff2") format('woff2');
        unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
      }
      /* greek */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 400;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-greek.woff2") format('woff2');
        unicode-range: U+0370-0377, U+037A-037F, U+0384-038A, U+038C, U+038E-03A1, U+03A3-03FF;
      }
      /* vietnamese */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 400;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-vietnamese.woff2") format('woff2');
        unicode-range: U+0102-0103, U+0110-0111, U+0128-0129, U+0168-0169, U+01A0-01A1, U+01AF-01B0, U+0300-0301, U+0303-0304, U+0308-0309, U+0323, U+0329, U+1EA0-1EF9, U+20AB;
      }
      /* latin-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 400;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin-ext.woff2") format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+0304, U+0308, U+0329, U+1D00-1DBF, U+1E00-1E9F, U+1EF2-1EFF, U+2020, U+20A0-20AB, U+20AD-20C0, U+2113, U+2C60-2C7F, U+A720-A7FF;
      }
      /* latin */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 400;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin.woff2") format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      /* cyrillic-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 500;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic-ext.woff2") format('woff2');
        unicode-range: U+0460-052F, U+1C80-1C8A, U+20B4, U+2DE0-2DFF, U+A640-A69F, U+FE2E-FE2F;
      }
      /* cyrillic */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 500;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic.woff2") format('woff2');
        unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
      }
      /* greek */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 500;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-greek.woff2") format('woff2');
        unicode-range: U+0370-0377, U+037A-037F, U+0384-038A, U+038C, U+038E-03A1, U+03A3-03FF;
      }
      /* vietnamese */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 500;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-vietnamese.woff2") format('woff2');
        unicode-range: U+0102-0103, U+0110-0111, U+0128-0129, U+0168-0169, U+01A0-01A1, U+01AF-01B0, U+0300-0301, U+0303-0304, U+0308-0309, U+0323, U+0329, U+1EA0-1EF9, U+20AB;
      }
      /* latin-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 500;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin-ext.woff2") format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+0304, U+0308, U+0329, U+1D00-1DBF, U+1E00-1E9F, U+1EF2-1EFF, U+2020, U+20A0-20AB, U+20AD-20C0, U+2113, U+2C60-2C7F, U+A720-A7FF;
      }
      /* latin */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 500;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin.woff2") format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      /* cyrillic-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 600;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic-ext.woff2") format('woff2');
        unicode-range: U+0460-052F, U+1C80-1C8A, U+20B4, U+2DE0-2DFF, U+A640-A69F, U+FE2E-FE2F;
      }
      /* cyrillic */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 600;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic.woff2") format('woff2');
        unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
      }
      /* greek */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 600;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-greek.woff2") format('woff2');
        unicode-range: U+0370-0377, U+037A-037F, U+0384-038A, U+038C, U+038E-03A1, U+03A3-03FF;
      }
      /* vietnamese */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 600;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-vietnamese.woff2") format('woff2');
        unicode-range: U+0102-0103, U+0110-0111, U+0128-0129, U+0168-0169, U+01A0-01A1, U+01AF-01B0, U+0300-0301, U+0303-0304, U+0308-0309, U+0323, U+0329, U+1EA0-1EF9, U+20AB;
      }
      /* latin-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 600;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin-ext.woff2") format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+0304, U+0308, U+0329, U+1D00-1DBF, U+1E00-1E9F, U+1EF2-1EFF, U+2020, U+20A0-20AB, U+20AD-20C0, U+2113, U+2C60-2C7F, U+A720-A7FF;
      }
      /* latin */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 600;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin.woff2") format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }
      /* cyrillic-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 700;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic-ext.woff2") format('woff2');
        unicode-range: U+0460-052F, U+1C80-1C8A, U+20B4, U+2DE0-2DFF, U+A640-A69F, U+FE2E-FE2F;
      }
      /* cyrillic */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 700;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-cyrillic.woff2") format('woff2');
        unicode-range: U+0301, U+0400-045F, U+0490-0491, U+04B0-04B1, U+2116;
      }
      /* greek */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 700;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-greek.woff2") format('woff2');
        unicode-range: U+0370-0377, U+037A-037F, U+0384-038A, U+038C, U+038E-03A1, U+03A3-03FF;
      }
      /* vietnamese */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 700;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-vietnamese.woff2") format('woff2');
        unicode-range: U+0102-0103, U+0110-0111, U+0128-0129, U+0168-0169, U+01A0-01A1, U+01AF-01B0, U+0300-0301, U+0303-0304, U+0308-0309, U+0323, U+0329, U+1EA0-1EF9, U+20AB;
      }
      /* latin-ext */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 700;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin-ext.woff2") format('woff2');
        unicode-range: U+0100-02BA, U+02BD-02C5, U+02C7-02CC, U+02CE-02D7, U+02DD-02FF, U+0304, U+0308, U+0329, U+1D00-1DBF, U+1E00-1E9F, U+1EF2-1EFF, U+2020, U+20A0-20AB, U+20AD-20C0, U+2113, U+2C60-2C7F, U+A720-A7FF;
      }
      /* latin */
      @font-face {
        font-family: 'IBM Plex Sans';
        font-style: normal;
        font-weight: 700;
        font-stretch: 100%;
        font-display: swap;
        src: url("/fonts/ibm-plex-sans-400-latin.woff2") format('woff2');
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
      }

      /* The palette the design export expressed as a literal oklch() on every
         element it touched. Named here once so a hue change is one edit, and
         so the three library accents are declared next to each other rather
         than inferred from repetition. */
      :root {
        --ink: oklch(20% 0.01 260);
        --code-ink: oklch(35% 0.01 260);
        --muted: oklch(45% 0.01 260);
        --soft: oklch(55% 0.01 260);
        --line: oklch(91% 0.005 260);
        --rule: oklch(92% 0.005 260);
        --page: oklch(99% 0.002 90);
        --surface: oklch(97.5% 0.003 260);
        --panel: oklch(97% 0.005 260);
        --on-dark: oklch(96% 0.005 260);
        --red: oklch(60% 0.19 25);
        --green: oklch(65% 0.17 145);
        --blue: oklch(55% 0.18 260);
        --mono: 'IBM Plex Mono', monospace;
      }

      html { scroll-behavior: smooth; }
      body { margin: 0; background: var(--page); font-family: 'IBM Plex Sans', Helvetica, Arial, sans-serif; }
      a { color: var(--blue); text-decoration: none; }
      a:hover { color: oklch(45% 0.18 260); }
      * { box-sizing: border-box; }

      .page { color: var(--ink); line-height: 1.5; }
      .wrap { max-width: 1072px; margin: 0 auto; }

      /* Section labels. The copy is stored title-case in src/data/landing.ts
         so the markdown twin reads naturally; the caps are presentational. */
      .eyebrow {
        font-family: var(--mono);
        font-size: 12px;
        color: var(--soft);
        margin-bottom: 10px;
        text-transform: uppercase;
      }
      .eyebrow-blue { color: var(--blue); }
      .eyebrow-red { color: var(--red); }
      /* Green-only Package Skills key their section labels to that accent. */
      .eyebrow-green { color: var(--green); }

      /* HERO */
      .hero {
        max-width: 1120px;
        margin: 0 auto;
        padding: 64px 24px 88px;
        display: grid;
        grid-template-columns: 1.1fr 0.9fr;
        gap: 56px;
        align-items: center;
      }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-family: var(--mono);
        font-size: 12px;
        color: var(--blue);
        background: oklch(96% 0.03 260);
        border: 1px solid oklch(90% 0.05 260);
        padding: 6px 12px;
        border-radius: 100px;
        margin-bottom: 24px;
        text-transform: uppercase;
      }
      h1 { font-size: 52px; line-height: 1.08; letter-spacing: -0.02em; font-weight: 600; margin: 0 0 20px; }
      .hero-lede { font-size: 18px; color: var(--muted); margin: 0 0 32px; max-width: 520px; }

      .install { display: flex; flex-direction: column; gap: 12px; max-width: 480px; }
      .install-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: var(--ink);
        border-radius: 8px;
        padding: 14px 16px;
      }
      /* The CTA repeat shrink-wraps its content instead of filling a column. */
      .install-row-inline {
        display: inline-flex;
        justify-content: flex-start;
        gap: 10px;
        padding: 14px 20px;
        margin: 0 auto;
      }
      .install-row code { font-family: var(--mono); font-size: 14px; color: var(--on-dark); }
      .copy {
        background: oklch(30% 0.01 260);
        color: var(--on-dark);
        border: none;
        border-radius: 6px;
        padding: 6px 12px;
        font-size: 12px;
        font-family: var(--mono);
        cursor: pointer;
      }
      .copy:hover { background: oklch(35% 0.01 260); }
      .install-note { font-size: 13px; color: var(--soft); }

      .yml {
        background: var(--panel);
        border: 1px solid var(--line);
        border-radius: 12px;
        padding: 28px;
        font-family: var(--mono);
        font-size: 13px;
        color: var(--code-ink);
      }
      .yml-caption { color: var(--soft); margin-bottom: 14px; font-size: 12px; }

      /* SECTIONS */
      .sec { border-top: 1px solid var(--rule); padding: 72px 24px; }
      .featured-intro { padding-top: 64px; border-top: 0; }
      .featured-intro > .wrap { display: grid; grid-template-columns: minmax(0, .9fr) minmax(0, 1.1fr); gap: 56px; align-items: start; }
      .featured-intro-copy .sec-lede { margin-bottom: 0; }
      .featured-toc { padding: 24px; border: 1px solid var(--line); border-radius: 12px; background: var(--surface); }
      .featured-toc-head { display: flex; align-items: baseline; justify-content: space-between; gap: 20px; margin-bottom: 18px; }
      .featured-toc-title { margin: 0; color: var(--ink); font-size: 15px; font-weight: 600; }
      .featured-toc-count { color: var(--soft); font-size: 13px; }
      .featured-toc-links { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; }
      .featured-toc a { display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 9px; min-width: 0; padding: 11px 12px; border: 1px solid var(--line); border-radius: 8px; background: var(--page); color: var(--ink); font-size: 14px; transition: border-color 150ms ease, background-color 150ms ease, color 150ms ease; }
      .featured-toc a:hover { border-color: oklch(80% 0.04 260); background: oklch(97% 0.015 260); color: var(--blue); }
      .featured-toc-index { color: var(--soft); font: 11px var(--mono); }
      .featured-toc-arrow { color: var(--blue); }
      .sec-alt { background: var(--surface); }
      .sec h2 { font-size: 28px; font-weight: 600; letter-spacing: -0.01em; margin: 0 0 12px; }
      /* A heading with no lede under it carries the lede's bottom margin. */
      .sec h2.solo { margin-bottom: 40px; }
      .sec-lede { font-size: 16px; color: var(--muted); margin: 0 0 40px; max-width: 640px; }
      .sec-lede-wide { max-width: 680px; }
      .definition p { max-width: 900px; margin: 0; color: var(--muted); font-size: 16px; }

      .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
      .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
      .grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 48px; }
      /* K3s and Walter each have five steps. Their own class rather than a
         wider .grid-4, because .grid-4 lays out the four Once steps and they
         must keep their width. Five tracks in the same 1120px wrap give a
         narrower card, so keep these bodies short. */
      .grid-5 { display: grid; grid-template-columns: repeat(5, 1fr); gap: 16px; margin-bottom: 48px; }

      /* Cards — the browser/package pair. */
      .card { border: 1px solid var(--line); border-radius: 12px; padding: 28px; }
      .card h3 { font-size: 19px; font-weight: 600; margin: 0 0 10px; }
      .card p { font-size: 15px; color: var(--muted); margin: 0; }
      .card-accent { border-color: oklch(85% 0.06 260); background: oklch(97% 0.02 260); }
      .card-accent p { color: oklch(40% 0.01 260); }

      /* Cards — what a Package Skill bundles. */
      .bundle { background: var(--page); border: 1px solid var(--line); border-radius: 12px; padding: 24px; }
      .bundle .eyebrow { margin-bottom: 12px; }
      .bundle h3 { font-size: 17px; font-weight: 600; margin: 0 0 8px; }
      .bundle p { font-size: 14px; color: var(--muted); margin: 0; }

      /* Cards — the three libraries. */
      .lib {
        display: flex;
        flex-direction: column;
        background: var(--surface);
        border: 1px solid var(--line);
        border-radius: 12px;
        padding: 24px;
        color: inherit;
      }
      .lib:hover { border-color: oklch(80% 0.01 260); }
      .lib-swatch { width: 32px; height: 32px; border-radius: 8px; margin-bottom: 16px; }
      .lib h3 { font-size: 17px; font-weight: 600; margin: 0 0 4px; font-family: var(--mono); }
      .lib-stack { font-size: 13px; color: var(--soft); margin-bottom: 12px; }
      .lib p { font-size: 14px; color: var(--muted); margin: 0 0 12px; }

      /* Cards — the four Once steps. */
      .step { border: 1px solid var(--line); border-radius: 10px; padding: 20px; background: var(--page); }
      .step .eyebrow { font-size: 11px; margin-bottom: 8px; }
      .step h4 { font-size: 15px; font-weight: 600; margin: 0 0 6px; }
      .step p { font-size: 13px; color: var(--muted); margin: 0; }

      /* DAG */
      .dag-panel { border: 1px solid var(--line); border-radius: 12px; padding: 36px; background: var(--page); }
      .dag-caption { font-family: var(--mono); font-size: 12px; color: var(--soft); margin-bottom: 24px; }
      .dag { display: flex; align-items: center; gap: 0; flex-wrap: wrap; font-family: var(--mono); font-size: 13px; }
      .dag-node { background: white; border: 1px solid oklch(85% 0.005 260); padding: 10px 16px; border-radius: 8px; }
      /* No border, matching the export — it sits 2px shorter than its
         neighbours and align-items:center absorbs the difference. */
      .dag-start { background: var(--ink); color: white; border: none; }
      .dag-edge { width: 32px; height: 1px; background: oklch(80% 0.005 260); }
      .dag-group { display: flex; flex-direction: column; gap: 8px; }
      .dag-branch { display: grid; grid-template-columns: auto auto; gap: 8px 0; align-items: center; }
      .dag-tail { display: flex; align-items: center; }
      .dag-note { font-size: 13px; color: var(--muted); margin: 20px 0 0; }
      /* Long graphs keep their desktop flow on one line and scroll only the
         diagram, not the panel. */
      @media (min-width: 861px) {
        .wide-dag {
          flex-wrap: nowrap;
          overflow-x: auto;
          padding-bottom: 8px;
        }
        .wide-dag > * { flex: none; }
      }
      /* Walter shows two panels — create/build, then the power pair — and the
         power panel holds two graphs under one caption. */
      .dag-panel + .dag-panel { margin-top: 20px; }
      .dag + .dag { margin-top: 16px; }
      .sec-install { margin: 0 0 40px; }
      .sec-install .install-note { display: block; margin: 10px 0 0; }

      /* CTA */
      .cta { border-top: 1px solid var(--rule); padding: 80px 24px; text-align: center; }
      .cta-inner { max-width: 640px; margin: 0 auto; }
      .cta h2 { font-size: 30px; font-weight: 600; letter-spacing: -0.01em; margin: 0 0 16px; }
      .cta p { font-size: 16px; color: var(--muted); margin: 0 0 32px; }
      .catalog-link { display: inline-flex; align-items: center; gap: 8px; padding: 14px 20px; border-radius: 8px; background: var(--ink); color: var(--on-dark); font: 13px var(--mono); }
      .catalog-link svg { width: 15px; height: 15px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
      .catalog-link:hover { background: oklch(30% 0.01 260); color: white; }

      /* Shiki emits <pre><code>, and both pick up the UA monospace family
         rather than inheriting the panel's. Reapply it, or the hero's YAML
         stops matching every other block of code on the page. */
      .astro-code,
      .astro-code code {
        font-family: var(--mono);
        font-size: 13px;
      }

      /* Responsive collapse. The export ships no media queries; these rules
         only take effect below 860px and leave the desktop design untouched.
         They no longer need !important — the declarations they override are
         now in this stylesheet rather than in style attributes, so ordinary
         cascade order settles it. Keep these blocks last. */
      @media (max-width: 860px) {
        .featured-intro > .wrap { grid-template-columns: 1fr; gap: 38px; }
        .featured-toc-links { grid-template-columns: repeat(2, minmax(0, 1fr)); }
        .hero,
        .grid-2,
        .grid-3 {
          grid-template-columns: 1fr;
        }
        .grid-4,
        .grid-5 {
          grid-template-columns: repeat(2, 1fr);
        }
        h1 {
          font-size: 38px;
        }
        /* Once the hero is a single column, the yaml panel shares the track
           with the copy block, and a grid item's automatic minimum size is its
           min-content width -- here the longest unwrapped line of colors.yml.
           That widens the track past the viewport and pushes every section's
           right padding off screen. min-width:0 lets the panel shrink to the
           track; the Shiki <pre> inside it already scrolls (overflow-x:auto,
           tabindex=0), so the long image: line stays reachable. Scoped to the
           collapsed layout: above 860px the two-column track sizing is
           load-bearing for the panel's width and must not change. */
        .yml {
          min-width: 0;
        }
      }
      @media (max-width: 520px) {
        .featured-toc { padding: 18px; }
        .featured-toc-head { align-items: flex-start; flex-direction: column; gap: 3px; }
        .featured-toc-links { grid-template-columns: 1fr; }
        .grid-4,
        .grid-5 {
          grid-template-columns: 1fr;
        }
        /* Matches the specificity of the rules above rather than relying on
           `code { ... !important }`, which is what this replaced. */
        .install-row code,
        .astro-code,
        .astro-code code {
          font-size: 12px;
        }
      }
      @media (prefers-reduced-motion: reduce) {
        html { scroll-behavior: auto; }
      }

      /* The DAG is a horizontal flex row with flex-wrap on desktop. Left to
         wrap on a narrow screen it breaks mid-flow and strands the connector
         lines at the end of a row, so below 860px it becomes a vertical
         sequence instead: the row turns into a column and each connector
         rotates from a 32x1 horizontal rule into a 1x20 vertical one. The
         stacked pairs (tofu-compute/tofu-smtp, ansible-local/ansible-remote)
         stay stacked -- that is what marks them as running in parallel.

         The ansible pair is a 2x2 grid rather than a plain column: github
         follows ansible-remote alone, so it sits in a second grid column on
         that row only, and the empty cell above it keeps the two ansible boxes
         one width. Below 860px the grid collapses to one column, the empty
         cell is dropped so it does not leave a gap, and the branch reads as a
         straight continuation of the vertical sequence. */
      @media (max-width: 860px) {
        .dag {
          flex-direction: column;
          align-items: stretch;
          flex-wrap: nowrap;
        }
        .dag > div {
          text-align: center;
        }
        .dag-edge {
          width: 1px;
          height: 20px;
          margin: 0 auto;
          flex: none;
        }
        .dag-group,
        .dag-branch {
          width: 100%;
          grid-template-columns: 1fr;
        }
        .dag-spacer {
          display: none;
        }
        .dag-tail {
          flex-direction: column;
          align-items: stretch;
        }
        .dag-panel {
          padding: 24px 20px;
        }
      }
    </style>
  </head>
  <body>
    <div class="page" id="top">

      <!-- NAV -->
      <SiteHeader active="featured" />

      <!-- FEATURED PACKAGE SKILLS -->
      <div class="sec featured-intro">
        <div class="wrap">
          <div class="featured-intro-copy">
            <div class="pill"><span>Curated by Colors</span></div>
            <h1>Featured</h1>
            <p class="sec-lede sec-lede-wide">Production examples that show what deterministic, agent-operated infrastructure looks like—from Kubernetes and data platforms to application hosting and remote development.</p>
          </div>
          <nav class="featured-toc" aria-label="Featured Package Skills on this page">
            <div class="featured-toc-head">
              <p class="featured-toc-title">On this page</p>
              <span class="featured-toc-count">{featuredPackageSkills.length} Package Skills</span>
            </div>
            <div class="featured-toc-links">
              {featuredPackageSkills.map((item, index) => (
                <a href={item.href}>
                  <span class="featured-toc-index">{String(index + 1).padStart(2, "0")}</span>
                  <span>{item.label}</span>
                  <span class="featured-toc-arrow" aria-hidden="true">↓</span>
                </a>
              ))}
            </div>
          </nav>
        </div>
      </div>

      <!-- K8S: FIRST EXAMPLE PACKAGE -->
      <div class="sec" id="k8s">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{k8s.eyebrow}</div>
          <SectionHeading id="k8s" title={k8s.heading} />
          <p class="sec-lede sec-lede-wide">{k8s.lede}</p>

          <div class="sec-install">
            <InstallBox command={k8sInstallCmd} id="featured-k8s-install" />
            <span class="install-note" set:html={html(k8s.runtimeNote)} />
            <ResourceLinks docsUrl={k8s.docsUrl} repoUrl={k8s.repoUrl} catalogUrl="/getcolors/k8s" />
          </div>

          <div class="grid-5">
            {k8s.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <div class="dag-panel">
            <div class="dag-caption">{k8s.dagCaption}</div>
            <div class="dag">
              {k8s.dag.map((item) => {
                if (item.kind === "edge") return <div class="dag-edge"></div>;
                return (
                  <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                );
              })}
            </div>
            <p class="dag-note">{k8s.dagNote}</p>
          </div>
        </div>
      </div>

      <!-- K3S: SECOND EXAMPLE PACKAGE -->
      <div class="sec sec-alt" id="k3s">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{k3s.eyebrow}</div>
          <SectionHeading id="k3s" title={k3s.heading} />
          <p class="sec-lede sec-lede-wide">{k3s.lede}</p>

          <div class="sec-install">
            <InstallBox command={k3sInstallCmd} id="featured-k3s-install" />
            <span class="install-note" set:html={html(k3s.runtimeNote)} />
            <ResourceLinks docsUrl={k3s.docsUrl} repoUrl={k3s.repoUrl} catalogUrl="/getcolors/k3s" />
          </div>

          <div class="grid-5">
            {k3s.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <div class="dag-panel">
            <div class="dag-caption">{k3s.dagCaption}</div>
            <div class="dag">
              {k3s.dag.map((item) => {
                if (item.kind === "edge") return <div class="dag-edge"></div>;
                if (item.kind === "group")
                  return (
                    <div class="dag-group">
                      {item.nodes.map((n) => <div class="dag-node">{n}</div>)}
                    </div>
                  );
                return (
                  <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                );
              })}
            </div>
            <p class="dag-note">{k3s.dagNote}</p>
          </div>
        </div>
      </div>

      <!-- CLICKHOUSE: THIRD EXAMPLE PACKAGE -->
      <div class="sec" id="clickhouse">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{clickhouse.eyebrow}</div>
          <SectionHeading id="clickhouse" title={clickhouse.heading} />
          <p class="sec-lede sec-lede-wide">{clickhouse.lede}</p>

          <div class="sec-install">
            <InstallBox command={clickhouseInstallCmd} id="featured-clickhouse-install" />
            <span class="install-note" set:html={html(clickhouse.runtimeNote)} />
            <ResourceLinks docsUrl={clickhouse.docsUrl} repoUrl={clickhouse.repoUrl} catalogUrl="/getcolors/clickhouse" />
          </div>

          <div class="grid-5">
            {clickhouse.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <div class="dag-panel">
            <div class="dag-caption">{clickhouse.dagCaption}</div>
            <div
              class="dag wide-dag"
              tabindex="0"
              aria-label="ClickHouse create and build workflow diagram"
            >
              {clickhouse.dag.map((item) => {
                if (item.kind === "edge") return <div class="dag-edge"></div>;
                if (item.kind === "group")
                  return (
                    <div class="dag-group">
                      {item.nodes.map((n) => <div class="dag-node">{n}</div>)}
                    </div>
                  );
                return (
                  <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                );
              })}
            </div>
            <p class="dag-note">{clickhouse.dagNote}</p>
          </div>
        </div>
      </div>

      <!-- AIRFLOW: FOURTH EXAMPLE PACKAGE -->
      <div class="sec sec-alt" id="airflow">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{airflow.eyebrow}</div>
          <SectionHeading id="airflow" title={airflow.heading} />
          <p class="sec-lede sec-lede-wide">{airflow.lede}</p>

          <div class="sec-install">
            <InstallBox command={airflowInstallCmd} id="featured-airflow-install" />
            <span class="install-note" set:html={html(airflow.runtimeNote)} />
            <ResourceLinks docsUrl={airflow.docsUrl} repoUrl={airflow.repoUrl} catalogUrl="/getcolors/airflow" />
          </div>

          <div class="grid-5">
            {airflow.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <div class="dag-panel">
            <div class="dag-caption">{airflow.dagCaption}</div>
            <div
              class="dag wide-dag"
              tabindex="0"
              aria-label="Airflow create and build workflow diagram"
            >
              {airflow.dag.map((item) => {
                if (item.kind === "edge") return <div class="dag-edge"></div>;
                if (item.kind === "node")
                  return (
                    <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                  );
                return (
                  <div class="dag-branch">
                    <div class="dag-node">{item.nodes[0]}</div>
                    <div class="dag-spacer"></div>
                    <div class="dag-node">{item.nodes[1]}</div>
                    <div class="dag-tail">
                      <div class="dag-edge"></div>
                      <div class="dag-node">{item.tail}</div>
                    </div>
                  </div>
                );
              })}
            </div>
            <p class="dag-note">{airflow.dagNote}</p>
          </div>
        </div>
      </div>

      <!-- RAMA: FIFTH EXAMPLE PACKAGE -->
      <div class="sec" id="rama">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{rama.eyebrow}</div>
          <SectionHeading id="rama" title={rama.heading} />
          <p class="sec-lede sec-lede-wide">{rama.lede}</p>

          <div class="sec-install">
            <InstallBox command={ramaInstallCmd} id="featured-rama-install" />
            <span class="install-note" set:html={html(rama.runtimeNote)} />
            <ResourceLinks docsUrl={rama.docsUrl} repoUrl={rama.repoUrl} catalogUrl="/getcolors/rama" />
          </div>

          <div class="grid-5">
            {rama.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <div class="dag-panel">
            <div class="dag-caption">{rama.dagCaption}</div>
            <div
              class="dag wide-dag"
              tabindex="0"
              aria-label="Rama create and build workflow diagram"
            >
              {rama.dag.map((item) => {
                if (item.kind === "edge") return <div class="dag-edge"></div>;
                return (
                  <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                );
              })}
            </div>
            <p class="dag-note">{rama.dagNote}</p>
          </div>
        </div>
      </div>

      <!-- ONCE: EXAMPLE PACKAGE -->
      <div class="sec sec-alt" id="once">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{once.eyebrow}</div>
          <SectionHeading id="once" title={once.heading} />
          <p class="sec-lede sec-lede-wide">{once.lede}</p>

          <div class="sec-install">
            <InstallBox command={installCmd} id="featured-once-install" />
            <span class="install-note" set:html={html(once.runtimeNote)} />
            <ResourceLinks docsUrl={once.docsUrl} repoUrl={once.repoUrl} catalogUrl="/getcolors/once" />
          </div>

          <div class="grid-4">
            {once.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <!-- DAG diagram -->
          <div class="dag-panel">
            <div class="dag-caption">{once.dagCaption}</div>
            <div class="dag">
              {once.dag.map((item) => {
                if (item.kind === "edge") return <div class="dag-edge"></div>;
                if (item.kind === "node")
                  return (
                    <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                  );
                if (item.kind === "group")
                  return (
                    <div class="dag-group">
                      {item.nodes.map((n) => <div class="dag-node">{n}</div>)}
                    </div>
                  );
                return (
                  <div class="dag-branch">
                    <div class="dag-node">{item.nodes[0]}</div>
                    <div class="dag-spacer"></div>
                    <div class="dag-node">{item.nodes[1]}</div>
                    <div class="dag-tail">
                      <div class="dag-edge"></div>
                      <div class="dag-node">{item.tail}</div>
                    </div>
                  </div>
                );
              })}
            </div>
            <p class="dag-note">{once.dagNote}</p>
          </div>
        </div>
      </div>

      <!-- VAULTWARDEN -->
      <div class="sec" id="vaultwarden">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{vaultwarden.eyebrow}</div>
          <SectionHeading id="vaultwarden" title={vaultwarden.heading} />
          <p class="sec-lede sec-lede-wide">{vaultwarden.lede}</p>

          <div class="sec-install">
            <InstallBox command={vaultwardenInstallCmd} id="featured-vaultwarden-install" />
            <span class="install-note" set:html={html(vaultwarden.runtimeNote)} />
            <ResourceLinks docsUrl={vaultwarden.docsUrl} repoUrl={vaultwarden.repoUrl} catalogUrl="/getcolors/vaultwarden" />
          </div>

          <div class="grid-5">
            {vaultwarden.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <div class="dag-panel">
            <div class="dag-caption">{vaultwarden.dagCaption}</div>
            <div class="dag">
              {vaultwarden.dag.map((item) => {
                if (item.kind === "edge") return <div class="dag-edge"></div>;
                if (item.kind === "node")
                  return (
                    <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                  );
                if (item.kind === "group")
                  return (
                    <div class="dag-group">
                      {item.nodes.map((n) => <div class="dag-node">{n}</div>)}
                    </div>
                  );
                return (
                  <div class="dag-branch">
                    <div class="dag-node">{item.nodes[0]}</div>
                    <div class="dag-spacer"></div>
                    <div class="dag-node">{item.nodes[1]}</div>
                    <div class="dag-tail">
                      <div class="dag-edge"></div>
                      <div class="dag-node">{item.tail}</div>
                    </div>
                  </div>
                );
              })}
            </div>
            <p class="dag-note">{vaultwarden.dagNote}</p>
          </div>
        </div>
      </div>

      <!-- WALTER -->
      <div class="sec sec-alt" id="walter">
        <div class="wrap">
          <div class="eyebrow eyebrow-blue">{walter.eyebrow}</div>
          <SectionHeading id="walter" title={walter.heading} />
          <p class="sec-lede sec-lede-wide" set:html={html(walter.lede)} />

          <div class="sec-install">
            <InstallBox command={walterInstallCmd} id="featured-walter-install" />
            <span class="install-note" set:html={html(walter.runtimeNote)} />
            <ResourceLinks docsUrl={walter.docsUrl} repoUrl={walter.repoUrl} catalogUrl="/getcolors/walter" />
          </div>

          <div class="grid-5">
            {walter.steps.map((s, i) => (
              <div class="step">
                <div class="eyebrow">{step(i)}</div>
                <h4>{s.title}</h4>
                <p set:html={html(s.body)} />
              </div>
            ))}
          </div>

          <!-- DAG diagrams: create/build, then the power pair -->
          {walter.dags.map((panel) => (
            <div class="dag-panel">
              <div class="dag-caption">{panel.caption}</div>
              {panel.graphs.map((items) => (
                <div class="dag">
                  {items.map((item) => {
                    if (item.kind === "edge") return <div class="dag-edge"></div>;
                    if (item.kind === "group")
                      return (
                        <div class="dag-group">
                          {item.nodes.map((n) => <div class="dag-node">{n}</div>)}
                        </div>
                      );
                    if (item.kind === "node")
                      return (
                        <div class:list={["dag-node", item.dark && "dag-start"]}>{item.label}</div>
                      );
                    return null;
                  })}
                </div>
              ))}
            </div>
          ))}
          <p class="dag-note">{walter.dagNote}</p>
        </div>
      </div>

      <!-- FINAL CTA -->
      <div class="cta">
        <div class="cta-inner">
          <h2>Find infrastructure your agent can operate.</h2>
          <p>Browse the PR-curated Package Skills Catalog by platform, provider, or runtime.</p>
          <a class="catalog-link" href="/skills"><svg aria-hidden="true" viewBox="0 0 24 24"><path d="m9 5 7 7-7 7" /></svg><span>View the Catalog</span></a>
        </div>
      </div>

      <!-- FOOTER -->
      <SiteFooter />

    </div>

    <script is:inline>
      document.querySelectorAll("[data-copy]").forEach((button) => {
        button.addEventListener("click", () => {
          navigator.clipboard?.writeText(button.dataset.copy);
          button.textContent = "Copied";
          setTimeout(() => (button.textContent = "Copy"), 1500);
        });
      });
    </script>
  </body>
</html>
