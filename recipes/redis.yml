name: Redis
repository: getcolors/redis
summary: Operate one Redis 7.2 server on one Vultr instance — published on loopback and the VPC address only, reached over an SSH tunnel, with an append-only file for persistence and RDB backup sets in Cloudflare R2 that a rehearsal verb restores and reads back.
keywords:
  - Redis
  - cache
  - queue
  - key-value store
  - append-only file
  - AOF
  - noeviction
  - RDB backup
  - Cloudflare R2
  - Vultr
  - SSH tunnel
  - single node
  - self-hosted
package-skills:
  - name: package-redis-green
    path: skills/package-redis-green/SKILL.md
    runtime: green
